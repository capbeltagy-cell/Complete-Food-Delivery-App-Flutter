import'package:dierb_api_client/dierb_api_client.dart';import'package:flutter/material.dart';class RiderEarningsPage extends StatefulWidget{const RiderEarningsPage({super.key});@override State<RiderEarningsPage>createState()=>_State();}class _State extends State<RiderEarningsPage>{final api=DierbApi();List<dynamic>orders=const[];bool loading=true;@override void initState(){super.initState();load();}@override void dispose(){api.close();super.dispose();}Future<void>load()async{final v=await api.orders();if(mounted)setState((){orders=v.where((x)=>(x as Map)['status']=='delivered').toList();loading=false;});}@override Widget build(BuildContext context){final deliveryValue=orders.fold<double>(0,(s,x)=>s+(((x as Map)['deliveryFeePiasters']as num?)?.toDouble()??0)/100);return Scaffold(appBar:AppBar(title:const Text('سجل التوصيل')),body:loading?const Center(child:CircularProgressIndicator()):ListView(padding:const EdgeInsets.all(16),children:[Container(padding:const EdgeInsets.all(22),decoration:BoxDecoration(gradient:const LinearGradient(colors:[Color(0xFF14532D),Color(0xFF22A060)]),borderRadius:BorderRadius.circular(24)),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('قيمة خدمات التوصيل المكتملة',style:TextStyle(color:Colors.white70)),Text('${deliveryValue.toStringAsFixed(2)} ج.م',style:const TextStyle(color:Colors.white,fontSize:30,fontWeight:FontWeight.w900)),Text('${orders.length} توصيل مكتمل',style:const TextStyle(color:Colors.white))])),const SizedBox(height:14),const Text('المستحقات النهائية تعتمد على إعداد عمولة المندوب في الإدارة.',style:TextStyle(color:Colors.black54)),...orders.map((x){final m=x as Map,store=m['store']as Map?;return Card(child:ListTile(leading:const CircleAvatar(child:Icon(Icons.check)),title:Text((store?['name']??'متجر ديرب').toString()),subtitle:Text('#${m['orderNumber']??m['id']}')));})]));}}
